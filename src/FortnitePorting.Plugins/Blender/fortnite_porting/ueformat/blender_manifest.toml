schema_version = "1.0.0"

type = "add-on"
license = ["SPDX:GPL-3.0-or-later"]

id = "io_scene_ueformat"
version = "0.10.0"
blender_version_min = "4.2.0"
name = "UEFormat (.uemodel / .ueanim / .uepose)"
tagline = "Importer for UEFormat Files"
maintainer = "https://github.com/h4lfheart"

tags = ["Import-Export"]